# SQLite. Versions 3.8.0 and up are supported.
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem "sqlite3"
#
# Databases live under db/ (the classic layout). `pool` is valid across the
# supported matrix (7.1/7.2/8.0; verified pool sizing on each) and the test
# env sets it explicitly: CacheTest spawns 8 concurrent reader threads plus
# the main thread, so the default pool of 5 exhausts under Rails 7.1
# (ActiveRecord::ConnectionTimeoutError) while 7.2/8.0 happen to pass on
# timing. Pool 15 covers 8 workers + main with headroom; sqlite handles it
# fine for the dummy app.
default: &default
  adapter: sqlite3
  timeout: 5000

development:
  <<: *default
  database: db/development.sqlite3

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: db/test.sqlite3
  pool: 15

# SQLite3 write its data on the local filesystem, as such it requires
# persistent disks. If you are deploying to a managed service, you should
# make sure it provides disk persistence, as many don't.
#
# Similarly, if you deploy your application as a Docker container, you must
# ensure the database is located in a persisted volume.
production:
  <<: *default
  # database: path/to/persistent/storage/production.sqlite3
